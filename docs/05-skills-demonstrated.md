# Skills Demonstrated

Lift these straight onto your CV / LinkedIn once each item is actually done.
Swap the brackets for your own wording.

## Networking
- Designed and subnetted a [____]-VLAN network from a single /24 using **VLSM**
- Configured **VLANs**, 802.1Q **trunking**, and a parked native VLAN across [5] switches
- Built redundant **LACP EtherChannel** uplinks between distribution and access
- Implemented **inter-VLAN routing** with SVIs on a Layer 3 switch
- Deployed single-area **OSPF** across two routers and a multilayer switch
- Configured **DHCP relay** to centralise addressing on a Windows server

## Systems / Helpdesk
- Stood up **Windows Server 2022** with **Active Directory, DNS, and DHCP**
- Created **OUs, security groups, and user accounts**; applied a **Group Policy Object**
- **Domain-joined** a Windows 11 client and validated authentication
- Deployed **GLPI** for ticketing/asset management and **Uptime Kuma** for monitoring
- Documented troubleshooting in a ticket-based runbook

## Tools
`Cisco IOS` · `VirtualBox` · `Windows Server 2022` · `Active Directory` · `PowerShell`
· `Docker` · `Raspberry Pi OS` · `GLPI` · `Uptime Kuma` · `Git`

## One-line summary for the top of a CV
> <!-- ✍️ -->
> Built a multi-VLAN Cisco network with OSPF, EtherChannel and inter-VLAN routing, then layered
> a Windows AD domain and helpdesk tooling on top — end to end, on real hardware.
